export const DEFAULT_NODE_ENV = 'development';

export const DEFAULT_APP_PORT = 5173;

export const DEFAULT_SSR_PORT = 3001;

export const DEFAULT_SERVICE_PORT = 9666;

export const DEFAULT_APP_URL = `http://localhost:${DEFAULT_APP_PORT}`;

export const DEFAULT_SSR_URL = `http://localhost:${DEFAULT_SSR_PORT}`;

export const DEFAULT_SERVICE_URL = `http://localhost:${DEFAULT_SERVICE_PORT}`;
