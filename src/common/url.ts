export const APP_PORT = 5173;

export const SSR_PORT = 3001;

export const SERVICE_PORT = 9666;

export const APP_URL = `http://localhost:${APP_PORT}`;

export const SSR_URL = `http://localhost:${SSR_PORT}`;

export const SERVICE_URL = `http://localhost:${SERVICE_PORT}`;
